TITLE PROG TO DISP STRING THORUGH 21H
DATA SEGMENT
        STR1 DB 'HELLO$' ;STRING WHICH IS TO BE DIS.
DATA ENDS
CODE SEGMENT
        ASSUME CS:CODE,DS:DATA
START:  MOV AX,DATA
        MOV DS,AX
        MOV AH,09H
        LEA DX,STR1
        INT 21H
        MOV AX,4C00H
        INT 21H
CODE ENDS
        END START
